class ModelState {
  String photo;
  String title;
  String subtitle;
  int seenStatus;

  ModelState(
      {required this.photo,
      required this.title,
      required this.subtitle,
      required this.seenStatus});
}
